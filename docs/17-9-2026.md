# BanterCombat 1.1.0 — 17-9-2026

## Implementado nesta versão

- Locale `ptBR` / `enUS`
- Painel `/banter config` (enable, sync whisper/group, som, model 3D, duração, PvP-only, reset pos)
- Fila de pop-ups (máx. 8) + `/banter queue`
- Dedup CLEU 1.5s; early-out com `pvpOnly`
- Protocolo **v2** (aceita v1); broadcast PARTY/RAID/INSTANCE
- UI: drag+save pos, AnimationGroup, PlayerModel opcional, medalha, cores por raridade, ícone de classe (`|T` / CreateTextureMarkup)
- DB v2 scrub de raça/classe; settings em `BanterCombatDB.settings`
- Matriz épica/lendária expandida (quase todas as classes)
- `docs/RegressionTesting.md`

## Skills aplicadas

- `wow-addon-dev` — namespace, load order, SavedVariables, performance CLEU
- `wow-api-widget` — frame, drag, AnimationGroup, PlayerModel
- `wow-api-framexml` — UIFrameFade*, PixelUtil (guard), CreateTextureMarkup
- `wow-api-escape-sequences` — cores `|c` / WrapTextInColorCode, markup de classe
